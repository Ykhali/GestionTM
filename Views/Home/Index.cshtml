<!--@{
    ViewData["Title"] = "Home";
}
<style>
    .bouton{
        width: 140px;
        height: 50px;
        font-size: 26px;
        background-color: cornflowerblue;
        color: white;
        text-align: center;
        font-weight: bold;
    }
</style>

<h1 style="margin:auto;padding:3%;">Welcome to GestionTM</h1>

@if (ViewBag.Message != null)
{
    <p style="color: red;">@ViewBag.Message</p>
}

<div style="margin-bottom: 20px;">
    <button class="bouton" onclick="showForm('register')">Register</button>
    <button class="bouton" onclick="showForm('login')">Login</button>
</div>

<div id="registerForm" style="display: none;">
    <h2>Register</h2>
    <form method="post" asp-action="Register">
        <input type="email" name="email" placeholder="Email" required />
        <input type="password" name="password" placeholder="Password" required />
        <input type="text" name="phoneNumber" placeholder="Phone Number" required />
        <button type="submit">Register</button>
    </form>
</div>

<div id="loginForm" style="display: none;">
    <h2>Login</h2>
    <form method="post" asp-action="Login">
        <input type="email" name="email" placeholder="Email" required />
        <input type="password" name="password" placeholder="Password" required />
        <button type="submit">Login</button>
    </form>
</div>

<script>
    function showForm(formType) {
        document.getElementById('registerForm').style.display = formType === 'register' ? 'block' : 'none';
        document.getElementById('loginForm').style.display = formType === 'login' ? 'block' : 'none';
    }
</script>-->
@{
    ViewData["Title"] = "Home";
}
<!--<style>
    body {
        font-family: Arial, sans-serif;
        background-color: #f4f4f9;
        margin: 0;
        padding: 0;
        display: flex;
        flex-direction: column;
        align-items: center;
    }

    h1 {
        color: #333;
        text-align: center;
        margin: auto;
        padding: 20px;
        margin-top: 10px;
        background-color: darkgrey;
        color: white;
        border-radius: 10px;
        width: 100%;
        max-width: 800px;
    }

    p {
        font-size: 18px;
        color: red;
        text-align: center;
    }

    .button-container {
        display: flex;
        justify-content: center;
        gap: 20px;
        margin: 20px 0;
    }

    .bouton {
        width: 150px;
        height: 50px;
        font-size: 18px;
        background-color: darkgray;
        color: white;
        border: none;
        border-radius: 5px;
        cursor: pointer;
        transition: background-color 0.3s ease, transform 0.2s ease;
    }

        .bouton:hover {
            background-color: green;
            transform: scale(1.05);
        }

    .form-container {
        display: none;
        width: 100%;
        max-width: 500px;
        padding: 20px;
        background: white;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
        border-radius: 10px;
        margin: 20px auto;
    }

        .form-container h2 {
            text-align: center;
            color: #333;
            margin-bottom: 20px;
        }

    form {
        display: flex;
        flex-direction: column;
        gap: 15px;
    }

        form input {
            height: 40px;
            padding: 10px;
            font-size: 16px;
            border: 1px solid #ccc;
            border-radius: 5px;
        }

        form button {
            height: 45px;
            background-color: darkseagreen;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 18px;
            transition: background-color 0.3s ease, transform 0.2s ease;
        }

            form button:hover {
                background-color: green;
                transform: scale(1.05);
            }
</style>-->
<style>
    body {
        font-family: Arial, sans-serif;
        margin: 0;
        padding: 0;
        display: flex;
        flex-direction: column;
        align-items: center;
    }

    h1 {
        color: #333;
        text-align: center;
        margin: auto;
        padding: 20px;
        margin-top: 10px;
        background-color: rgba(0, 0, 0, 0.6); /* Fond semi-transparent */
        color: white;
        border-radius: 10px;
        width: 100%;
        max-width: 800px;
    }

    p {
        font-size: 18px;
        color: red;
        text-align: center;
    }

    .button-container {
        display: flex;
        justify-content: center;
        gap: 20px;
        margin: 20px 0;
    }

    .bouton {
        width: 150px;
        height: 50px;
        font-size: 18px;
        background-color: rgba(0, 0, 0, 0.7);
        color: white;
        border: none;
        border-radius: 5px;
        cursor: pointer;
        transition: background-color 0.3s ease, transform 0.2s ease;
    }

        .bouton:hover {
            background-color: green;
            transform: scale(1.05);
        }

    .form-container {
        display: none;
        width: 100%;
        max-width: 500px;
        padding: 20px;
        background: rgba(255, 255, 255, 0.9); /* Fond semi-transparent */
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
        border-radius: 10px;
        margin: 20px auto;
    }

        .form-container h2 {
            text-align: center;
            color: #333;
            margin-bottom: 20px;
        }

    form {
        display: flex;
        flex-direction: column;
        gap: 15px;
    }

        form input {
            height: 40px;
            padding: 10px;
            font-size: 16px;
            border: 1px solid #ccc;
            border-radius: 5px;
        }

        form button {
            height: 45px;
            background-color: darkseagreen;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 18px;
            transition: background-color 0.3s ease, transform 0.2s ease;
        }

            form button:hover {
                background-color: green;
                transform: scale(1.05);
            }
</style>


<h1>Welcome to GestionTM</h1>

@if (ViewBag.Message != null)
{
    <p>@ViewBag.Message</p>
}

<div class="button-container">
    <button class="bouton" onclick="showForm('register')">Register</button>
    <button class="bouton" onclick="showForm('login')">Login</button>
</div>

<div id="registerForm" class="form-container">
    <h2>Register</h2>
    <form method="post" asp-action="Register">
        <input type="email" name="email" placeholder="Email" required />
        <input type="password" name="password" placeholder="Password" required />
        <input type="text" name="phoneNumber" placeholder="Phone Number" required />
        <button type="submit">Register</button>
    </form>
</div>

<div id="loginForm" class="form-container">
    <h2>Login</h2>
    <form method="post" asp-action="Login">
        <input type="email" name="email" placeholder="Email" required />
        <input type="password" name="password" placeholder="Password" required />
        <button type="submit">Login</button>
    </form>
</div>

<script>
    function showForm(formType) {
        document.getElementById('registerForm').style.display = formType === 'register' ? 'block' : 'none';
        document.getElementById('loginForm').style.display = formType === 'login' ? 'block' : 'none';
    }
</script>



